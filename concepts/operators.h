#include "elements.h"

#ifndef OPERATORS
#define OPERATORS

/*
 *definition of operators in the prover
 */

int isSubset(struct object* a, struct object* b);
int objectEquivalence(struct object* a, struct object* b);

#endif

